# Task 2 — Settings 与安全 Credential

规格：DEVELOPMENT.md Task 2。保留 ISettingsStore、ICredentialStore、AppSettings。

- [x] 先写真实文件系统和 Windows DPAPI 测试，用虚构密钥；确认未实现时失败。
- [x] src/VoiceTyper.Windows/Storage/AtomicFile.cs：同目录 CreateNew 临时文件、flush、原子重命名，失败清理临时文件，取消不改旧文件。
- [x] src/VoiceTyper.Windows/Settings/JsonSettingsStore.cs：缺失/损坏/null 返回默认值，不吞掉权限和 IO 错误，读取不改写文件。
- [x] src/VoiceTyper.Windows/Security/DpapiCredentialStore.cs：CurrentUser 加密，按 key 的 SHA256 命名，密文独立存储、替换、删除；清理明文字节，不记录密钥。
- [x] 测试默认值、部分配置、坏 JSON、roundtrip、取消/失败保存、并发完整性、secret 不在 JSON/文件明文、DPAPI 独立解密验证、损坏密文、删除。
- [x] restore、Debug build 和 test；代码审查并记录结果。

实现位置可由构造函数注入；默认使用 LocalApplicationData/VoiceTyper。Task 2 不导入真实 key.txt，也不连接云端，不实现 UI。

