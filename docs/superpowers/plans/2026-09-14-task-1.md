# Task 1 实施计划

目标：按照 spec.md 第 16–20 节建立 Solution 和 Core Contract，不实现业务。

架构：App 引用 Core、Windows、Volcengine；Windows 和 Volcengine 仅引用 Core；Core 不引用平台或 Provider。使用 .NET 8、C# 12、WPF、x64、Nullable 和 ImplicitUsings。

- [x] 创建根目录 VoiceTyper.sln、Directory.Build.props、global.json 和 .gitignore。
- [x] 创建 src/VoiceTyper.App、VoiceTyper.Core、VoiceTyper.Windows、VoiceTyper.Volcengine 四个项目及对应引用。仅 App 使用 WPF；Windows 使用 net8.0-windows。
- [x] 按第 19 节原样声明音频和识别接口，补全它们引用的 record；按第 18 节声明 DictationState。为热键、目标窗口、上屏、Overlay、Settings、凭据和性能测量声明平台无关接口。不实现 Coordinator、Hook、网络、存储、采集或 UI。
- [x] 创建三个 xUnit 测试项目。Task 1 无业务可测，不添加恒真或枚举值镜像测试；用编译验证接口闭合和项目引用。
- [x] 将规格原样复制为 DEVELOPMENT.md，README 记录边界、契约语义、当前进度、验证命令和发现的后续规格问题。
- [x] 执行 dotnet restore、dotnet build VoiceTyper.sln -c Debug、dotnet test VoiceTyper.sln -c Debug，记录准确结果。完成后停止，不进入 Task 2。

规格问题：750 ms 覆盖式缓冲不能独自保证 800 ms 建连期间首部音频不丢失；Idle 关闭麦克风时，缓冲不能补回设备启动前尚未采集的音频；Starting 阶段松键尚无状态转换。保留既定接口和状态，本任务不解决行为冲突。

